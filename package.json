{
  "name": "tailwind-style-scss-framework",
  "version": "1.0.0",
  "scripts": {
    "build": "sass styles.scss styles.css --no-source-map",
    "watch": "sass --watch styles.scss styles.css --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.62.0"
  }
}
