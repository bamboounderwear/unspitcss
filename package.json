{
  "name": "lightweight-scss-framework",
  "version": "2.1.2",
  "description": "SCSS framework that fixes undefined $gap variable errors by properly defining $generic-gap-values.",
  "main": "index.html",
  "scripts": {
    "prebuild": "mkdir -p css",
    "build": "sass scss/main.scss css/framework.css --style=expanded",
    "watch": "sass --watch scss/main.scss css/framework.css --style=expanded"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "sass": "^1.62.0"
  }
}
