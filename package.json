{
  "name": "lightweight-scss-framework",
  "version": "2.1.1",
  "description": "A verified SCSS framework setup that compiles to css/ and deploys on Netlify, ensuring @use is before any rules.",
  "main": "index.html",
  "scripts": {
    "prebuild": "mkdir -p css",
    "build": "sass scss/main.scss css/framework.css --style=expanded",
    "watch": "sass --watch scss/main.scss css/framework.css --style=expanded"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "sass": "^1.62.0"
  }
}
