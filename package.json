{
  "name": "lightweight-scss-framework",
  "version": "2.0.0",
  "description": "SCSS framework compiling directly to a css/ folder in the project root for Netlify deployment.",
  "main": "index.html",
  "scripts": {
    "prebuild": "mkdir -p css",
    "build": "sass scss/main.scss css/framework.css --style=expanded"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "sass": "^1.62.0"
  }
}
